export PATH="$HOME/.local/bin:$PATH"

export PATH="$HOME/bin:$PATH"

# Open Code
export PATH="$HOME/.opencode/bin:$PATH"

# Default editor. .zshenv is read by every zsh -- login, interactive and
# script -- so this reaches git, kubectl edit, crontab and friends.
export EDITOR=vim
export VISUAL=vim
